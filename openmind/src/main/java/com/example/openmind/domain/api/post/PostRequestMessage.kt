package com.example.openmind.domain.api.post

import com.example.openmind.domain.api.params.RequestParams
import com.example.openmind.domain.model.post.Post
import com.example.openmind.ui.create_post.viewModel.CreatePostViewModel

class PostRequestMessage(postViewModel: CreatePostViewModel, params: RequestParams) {
//    val post: Post

    init {
//        post = postViewModel.createPost()

    }
}
